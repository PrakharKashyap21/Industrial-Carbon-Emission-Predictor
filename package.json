{
  "name": "industrial-carbon-emission-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && .venv/bin/uvicorn app.main:app --reload",
    "build": "npm --prefix frontend run build"
  }
}
